#include <stdio.h>

int main() {
    int n, i;
    scanf("%d", &n);

    for (i = 2; i <= n / 2; i++)
        if (n % i == 0) {
            printf("Not Prime");
            return 0;
        }

    if (n > 1)
        printf("Prime");
    else
        printf("Not Prime");

    return 0;
}
